1 - Задача решена верно, я использовал вывод всех полей по гномам, а также inner join вместо join. 
В случае postgres (и ms sql) это идентично, однако в других СУБД они могут отличаться, поэтому следует внимательно применять

2 - Задача решена верно, отличие в финальном наборе полей (я вывел все доступные поля из таблицы)

3 - В целом моё решение строилось верно, однако  допустил ошибку. 
В подзапросе, получаещем максимальный приоритет для задачи не указал статус pending, 
что могло привести к получению неправильного результата. В случае, если бы максимальный приоритет был бы со статусом, отличным от pending, 
я бы ошибочно получал в результирующем наборе пустую выборку.

4 - В данном решение в финальном результате я предположил, что необходимо получить лишь идентификатор гнома и количество его предметов. 
Для этого не использовал join, что может оказаться быстрее если бы этого было достаточно. Также использовал CTE для более наглядного получения предметов с владельцами. 
В решении же судя по всему ожидалось получить более детальную информацию по гномам, поэтому выполняется join с таблицей гномов и группировка.

5 - Основной идеей в данном решении было использование left join для включения информации об отрядах без гномов. 
Эту идею я уловил правильно, однако немного не докрутил решение - в count по невнимательности вывел количество отрядов, а не гномов.

6 - Моё решение в целом верное, не хватает сортировки по убыванию по задачам. В решении использовал join на CTE, получающее незавершённые задачи. 

7 - Моё решение соответствует результирующему, за исключением того, что я не использовал алиасы для колонок в финальном результате, 
что может быть полезно для представления информации

8 - Моё решение верно, но отличается от финального в подходе. В финальном решении используется таблица dwarwes c двумя подзапросами. 
В моём случае я вновь использовал CTE для получения среднего возраста гномов, а также left join c последующей фильтрацией. 
На больших объемах данных left join вероятнее может быть эффективнее подзапроса.