1) В правильной реализации получение статуса команды Left() в АТД TwoWayList(). 
В моей реализации ошибочно оставил получение данного статуса в АТД ParentList, что является неправильной реализацией АТД - 
статус команды необходимо получать там же, где и сама команда

2) В остальном, поскольку АТД LinkedList совпадает с АТД ParentList, 
команды, запросы, пред и постусловия сохранены относительно АТД LinkedList из задания 2.